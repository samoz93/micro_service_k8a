import { SamozExceptionFilter } from './exception-filter.filter';

describe('ExceptionFilterFilter', () => {
  it('should be defined', () => {
    expect(new SamozExceptionFilter()).toBeDefined();
  });
});
